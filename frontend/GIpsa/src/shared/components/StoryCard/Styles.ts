import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  container: {
    marginRight: 5,
    width: 100,
    height: 100,
    backgroundColor: '#555555',
  },
});

export default S;
