import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  container: {
    width: '100%',
  },
  title: {
    fontSize: 16,
    fontFamily: 'NotoSans',
  },
  storyCardContainer: {
    paddingBottom: 10,
  },
});

export default S;
