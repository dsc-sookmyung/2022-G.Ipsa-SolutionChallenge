import styled from 'styled-components/native';

import { colors } from 'shared/utils/colors';

export const Root = styled.View`
  margin-top: 30px;
`;

export const StoryContainer = styled.ScrollView`
  margin-top: 24px;
`;
