import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  container: {
    width: '100%',
    marginBottom: 36,
  },
  title: {
    fontSize: 16,
    fontFamily: 'NotoSans',
  },
  storyCardContainer: {
    marginTop: 16,
    paddingBottom: 8,
  },
});

export default S;
