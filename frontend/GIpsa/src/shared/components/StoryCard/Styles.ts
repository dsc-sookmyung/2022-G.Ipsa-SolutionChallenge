import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  container: { marginRight: 24 },
  thumbnail: {
    width: 160,
    height: 160,
    marginBottom: 16,
    borderRadius: 8,
  },
});

export default S;
