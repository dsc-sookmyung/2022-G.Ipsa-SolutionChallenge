import styled from 'styled-components/native';

import { colors } from 'shared/utils/colors';

export const Root = styled.View``;

export const TellerContainer = styled.ScrollView`
  margin-top: 24px;
`;
