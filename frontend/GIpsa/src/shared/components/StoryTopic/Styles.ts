import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  container: {
    width: '100%',
  },
  title: {
    fontSize: 16,
  },
  storyCardContainer: {
    paddingBottom: 10,
  },
});

export default S;
